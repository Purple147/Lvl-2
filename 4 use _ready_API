import requests
import webbrowser
q = requests.get("https://api.jikan.moe/v4/top/anime", proxies = {"http": "127.0.0.1:9090"})
q
ww = str(q.json()["data"][0]["title"])
ee = q.json()["data"][0]["url"]
b = "one punch man"
v = "one punch man comming"
print(ww, ee)
#webbrowser.open(ee)
if ww == b:
    print(v)
else:
    print(ww, "comming")
